# 英语学习小游戏

这是一个包含 16 个独立静态网页游戏的合集。打开仓库首页即可选择游戏。

## 本地运行

由于部分浏览器会限制 `file://` 下的音频和脚本，建议在本目录启动静态服务器：

```bash
python -m http.server 8000
```

然后访问 <http://localhost:8000/>。

## GitHub Pages

将仓库的 Pages 来源设置为 `main` 分支的根目录（`/(root)`），保存后即可发布。
